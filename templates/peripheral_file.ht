#ifndef ${INCLUDE_GUARD_TOKEN}
#define ${INCLUDE_GUARD_TOKEN}

#include "data/sbf/sbf.h"
#include <cstdint>

namespace ${NAMESPACE} {

static_assert(false, "This file has not been validated");

${PERIPHERAL}

} // namespace ${NAMESPACE}

#endif // ${INCLUDE_GUARD_TOKEN}
